# import cv2
# import numpy as np
# import face_recognition 
# import os 
#Variable_Declaration
        # self.var_dep=StringVar() 
        # self.var_course=StringVar() 
        # self.var_year=StringVar()    
        # self.var_sem=StringVar() 
        # self.var_name=StringVar()
        # self.var_enroll=StringVar() 
        # self.var_div=StringVar() 
        # self.var_gender=StringVar()    
        # self.var_dob=StringVar() 
        # self.var_email=StringVar()
        # self.var_phone=StringVar() 
        # self.var_address=StringVar() 
        # self.var_tgName=StringVar()    
        # self.var_photo=StringVar() 

        # textvariable=self.var_dep
        #"""or self.var_studentName.get()=="" or self.var_studentSNo.get()=="": """
        # INSERT INTO `face_recognizer`.`student` (`dep`, `course`, `year`, `semester`, `studentSNo`, `studentName`, `classSection`, `enrollNo`, `gender`, `studentDOB`, `emailId`, `phoneNo`, `address`, `tgName`, `photo`) VALUES ('CSE', 'B.Tech', '4th', '7th', '1', 'Rupesh', '0523cs191044', 'Section \" B \"', 'Male', '22-10-2000', 'er.rupesh.kc@gmaul.com', '8340795979', 'Neelbadh', 'Rahul Shrivasav', 'No');

#  CREATE TABLE `face_recognizer`.`student` (
#   `dep` VARCHAR(50) NULL,
#   `course` VARCHAR(45) NULL,
#   `year` VARCHAR(45) NULL,
#   `semester` VARCHAR(45) NULL,
#   `studentSNo` INT NOT NULL,
#   `studentName` VARCHAR(45) NULL,
#   `classSection` VARCHAR(15) NULL,
#   `enrollNo` VARCHAR(12) NULL,
#   `gender` VARCHAR(10) NULL,
#   `studentDOB` VARCHAR(45) NULL,
#   `emailId` VARCHAR(45) NULL,
#   `phoneNo` VARCHAR(12) NULL,
#   `address` VARCHAR(55) NULL,
#   `tgName` VARCHAR(45) NULL,
#   `photo` VARCHAR(10) NULL,
#   PRIMARY KEY (`studentSNo`));
